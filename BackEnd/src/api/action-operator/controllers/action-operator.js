'use strict';

/**
 * action-operator controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::action-operator.action-operator');
